<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.foodmanagement">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HistRecipeActivity"></activity>
        <activity android:name=".Test" />
        <activity android:name=".NewRecipe" />
        <activity android:name=".HistoryRecipe" />
        <activity android:name=".AddShopingListActivity" />
        <activity android:name=".RecipeActivity" />
        <activity android:name=".ShoppingListActivity" />
        <activity android:name=".addInventoryActivity" />
        <activity android:name=".FoodEdit" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>